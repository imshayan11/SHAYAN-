{
  "name": "image-server",
  "version": "1.0.0",
  "description": "An express server which has one API to return an image",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Javeria Lodhi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
